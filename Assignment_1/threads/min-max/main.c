#include "max_min.h"

int main()
{
  /* Find Maximum array element */
  printf("the maximum element  is %d",max_array());
  /* Find Minimum array element */
 printf("the minimum element  is %d",min_array());
  return 0;
}